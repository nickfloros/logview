const _ = require('lodash');

require('angular');

module.exports = angular.module('results-res-logs-controller-module', [
	
	])
	.controller('resLogsController', ['$rootScope', function($rootScope) {

		const ctrl = this;

		_.extends(ctrl, {
			
		});
	}]);