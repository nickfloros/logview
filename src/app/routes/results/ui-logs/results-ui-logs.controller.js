const _ = require('lodash');

require('angular');

module.exports = angular.module('results-ui-logs-controller-module', [
	
	])
	.controller('uiLogsController', ['$rootScope', function($rootScope) {

		const ctrl = this;

		_.extends(ctrl, {
			
		});
	}]);